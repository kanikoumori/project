# CMS Project

## 概要

卒業研究として開発するノーコードCMS（コンテンツ管理システム）です。
名前はsaiton
由来はサイトを作るのsiteから可愛い語尾をつけた


ユーザーはプログラミング知識がなくても、Webサイトを作成・編集・公開できます。

---

## 使用技術

* Laravel 12
* PHP 8.2
* PostgreSQL 16
* Laravel Breeze
* Blade
* Vite
* JavaScript
* CSS
* GitHub
* Render

---

## 開発メンバー

5人チーム開発

---

## 主な機能

### 実装中

* ユーザー認証
* サイト作成
* ページ作成
* ブロック管理
* プレビュー

### 今後実装予定

* 自動保存
* テーマ変更
* 公開機能
* アクセス解析
* フィードバック

---

## 環境構築

### Clone

```bash
git clone リポジトリURL
cd project
```

### Composer

```bash
composer install
```

### Node

```bash
npm install
```

### Environment

```bash
cp .env.example .env
php artisan key:generate
```

### Database

```bash
php artisan migrate
```

### 起動

```bash
php artisan serve
npm run dev
```

---

## 開発ルール

詳細は以下を参照。

```text
docs/git-rules.md
docs/coding-rules.md
docs/api-design.md
docs/database-design.md
docs/deployment.md
```

---

## ブランチ運用

```text
main
└ develop
    └ feature/*
```

---

## デプロイ

Renderを利用する。

詳細：

```text
docs/deployment.md
```
##ログイン機能

###新規アカウント作成

####概要
アカウントを持っていない場合は入力内容を入れて新規で作成し、ログイン状態でダッシュボードに移動する

####入力内容

-名前
-メールアドレス
-パスワード
-パスワード（確認）

#####パスワードの条件

8文字以上　英数字記号可能

####入力後

新規作成ボタンを押す
押すとユーザーが入力した
-名前
-メールアドレス
-パスワード
-パスワード確認がサーバーに送信される



#####phpで入力チェックを行う

名前が入力されているか
メールアドレスが入力されているか
メールアドレスの形式が正しいか
メールアドレスが重複していないか
パスワードが8文字以上か
パスワード確認と一致するか

#####エラーが出る条件

名前が未入力のとき
メールアドレスが未入力のとき
メールアドレス形式が不正なとき
メールアドレスが既に登録されているとき
パスワードが未入力のとき
パスワード確認欄が未入力のとき
パスワードと確認用パスワードが一致しないとき
パスワードが8文字未満のとき

#####入力チェックを通過した場合

エラーがなければパスワードをハッシュ化して保存し、usersテーブルにユーザー情報を登録する。
登録したユーザーをログイン状態にし、ダッシュボードへ移動する



###ログイン機能

####概要

ログイン認証成功後、LaravelはセッションIDを生成し、認証済みユーザーの情報をセッションに保存する。ブラウザにはCookieとしてセッションIDを保持させ、以降のアクセス時に送信されたセッションIDを基にユーザーを識別することでログイン状態を維持している。また、認証後はセッションIDを再生成し、セッション固定攻撃を防止している。

####入力内容

-メールアドレス
-パスワード

####ログイン状態を保持

セッション情報を利用することで、毎回メールアドレスとパスワードを入力することなくログイン状態を維持できる。


####入力完了

ログインのボタンを押す

####Laravel認証処理実行

LoginRequestで入力チェック

#####エラーが出る場合

メールアドレスが未入力のとき
パスワードが未入力のとき
メールアドレスが登録されていないとき
パスワードが間違っているとき

####usersテーブル照合

入力されたメールアドレスを使ってusersテーブルからユーザー情報を取得し、入力されたパスワードとDBに保存されているハッシュ化パスワードを照合する

####認証成功

ハッシュ値が一致した場合、新しいセッションを生成してサーバー側にユーザー情報を保存し、ログイン状態を維持したままダッシュボードへ遷移する。

セッションがあることでシステムは利用者を識別でき、ログイン状態を維持できる。



###パスワードを忘れた方はこちら

####概要

パスワードを忘れた場合、登録済みメールアドレス宛にパスワード再設定用URLを送信する。利用者はURLから新しいパスワードを設定できる。再設定時にはトークン認証を行い、本人確認後に users テーブルのパスワード情報を更新する。

####入力

ユーザーが登録済みのメールアドレスを入力して送信する

####usersテーブルを確認する

usersテーブルで入力されたメールアドレスが存在するか確認

#####エラーが出る場合

メールアドレスが未登録のとき

####パスワードリセット用トークンを作成

ランダムな文字列を生成し、パスワード再設定用トークンとしてデータベースに保存する

####リセットメールを送信

ユーザーへメールを送ります。
メール内にはURLが含まれます。
ユーザー本人しかメールを受信できないため、本人確認の役割もあります。

####メール内のURLをクリック

ユーザーがメールのリンクを開くと、
-新しいパスワード
-新しいパスワード（確認）
を入力する画面が表示されます。

####新しいパスワードを登録

問題なければ、ハッシュ化して users テーブルのパスワードを更新します。

#####エラーが出る場合

パスワード再設定用のURLが期限切れのとき
トークン認証に失敗したとき
新しいパスワードが条件を満たしていないとき
新しいパスワードと確認用パスワードが一致しないとき


####パスワード変更完了

古いパスワードは無効となり、新しいパスワードでのみログインできるようになる。